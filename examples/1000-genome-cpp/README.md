# 1000-genome example running with capio

## Compile CPP porting
1) install dependencied: 
`sudo apt install -y libboost-all-dev libcurl4-openssl-dev libarchive-dev`
2) Compile: `mkdir build && cd build && cmake .. && make -j`